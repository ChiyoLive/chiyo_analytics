repos:
  - repo: local
    hooks:
      - id: run-tests-pre-push
        name: Run all tests before push
        entry: uv run mng.py test all
        language: system
        stages: [pre-push]
        pass_filenames: false
        always_run: true
